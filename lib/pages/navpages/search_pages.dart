import 'package:flutter/material.dart';

class SearcPages extends StatelessWidget {
  const SearcPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Search Pages")),
    );
  }
}
